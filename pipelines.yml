
resources:
  - name: app2
    type: GitRepo
    repoPath: manishas/app
    configuration:
      gitProvider: manishas_github
      branches:
        include: master

pipelines:
  - name: barebones_pipeline
    steps:
      - name: step_1
        type: Bash
        configuration:
          inputResources:
            - app2
        execution:
          onExecute:
            - printenv

      - name: step_2
        type: Bash
        configuration:
          inputSteps:
            - step_1
        execution:
          onExecute:
            - printenv

      - name: step_3
        type: Bash
        configuration:
          inputSteps:
            - step_1
        execution:
          onExecute:
            - printenv

      - name: step_4
        type: Bash
        configuration:
          inputSteps:
            - step_2
            - step_3
        execution:
          onExecute:
            - printenv
